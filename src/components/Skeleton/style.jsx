import { makeStyles } from "@material-ui/core/styles";

const usesStyles = makeStyles(() => ({
  cards: {
    margin: "auto",
    maxWidth: 400,
    minHeight: 600,
    maxHeight: 600,
  },
  media: {
    height: 550,
  },
}));
export default usesStyles;
